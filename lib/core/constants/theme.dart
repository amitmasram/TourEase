import 'package:flutter/material.dart';
import 'colors.dart';

final ThemeData appTheme = ThemeData(
  primaryColor: AppColors.primary,
  hintColor: AppColors.secondary,
  // Define text themes and other theme settings
);
